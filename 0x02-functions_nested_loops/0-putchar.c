#include <unistd.h>
#include <stdio.h>
/**
 * main - Entry Point
 * Print _putchar using the main function
 * Return: 0 (Success)
 */

int main(voi)
{
	int c;
	char put[] = "_putchar";

	for (i = 0 ; i < 8 ; i++)
	{	putchar(put(i));
	}
	putchar('\n');
	return (0);
}
