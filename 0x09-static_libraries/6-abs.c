#include "main.h"

/**
 * _abs - Entry Point
 * Description: compute absolute value of n
 * @n: Inteer to be tested
 * Return: The absolute value of @n
 *
 */

int _abs(int n)
{
	return (n < 0 ? -1 * n : n);
}
